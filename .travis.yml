language: java
dist: xenial
sudo: required
jdk:
  - openjdk11

branches:
  only:
    - master

before_install:
  - chmod +x ./gradlew

# Travis CI 서버의 Home
cache:
  directories:
    - '$Home/ .m2/repository'
    - '$Home/ .gradle'


script: "./gradlew clean build"

# CI 실행 완료 시 메일로 알람.
notifications:
  email:
    recipients:
      - kimtaejun9705@gmail.com